{
  "name": "vehicle-orchestrator",
  "version": "1.0.0",
  "description": "Listens to sim.vehicles on NATS and dynamically creates/removes vehicle-agent containers",
  "main": "orchestrator.js",
  "scripts": {
    "start": "node orchestrator.js"
  },
  "dependencies": {
    "nats": "^2.28.2",
    "dockerode": "^4.0.4",
    "@kubernetes/client-node": "^0.22.3"
  }
}
